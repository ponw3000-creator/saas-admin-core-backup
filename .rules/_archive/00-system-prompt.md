# AI 架构师执行准则
1. **强制前置阅读**：在执行任何新建页面、修改组件或重构代码的任务前，必须先查阅 `.rules/` 目录下的所有相关规范。
2. **禁止局部补丁**：当出现 UI 对齐或布局错乱时，严禁使用 `margin`、`padding` 或 `vertical-align` 进行硬编码微调。必须从底层的 Flex/Grid 容器寻找物理原因并修正协议。
3. **架构思考输出**：在给出代码前，必须先输出【架构思考】，简述你应用了哪几条规范。