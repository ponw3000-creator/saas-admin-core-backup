# 响应式降级策略
1. **断点标准**：严格遵循 Element Plus 断点（xs: <768px, sm: ≥768px, md: ≥992px）。
2. **移动端强制单列**：在 `sm` 及以下屏幕，必须放弃多列 Flex/Grid，强制降级为 `flex-direction: column`。
3. **表单满宽**：移动端表单容器宽度必须为 `100%`，并在两侧保留 16px-20px 安全内边距。